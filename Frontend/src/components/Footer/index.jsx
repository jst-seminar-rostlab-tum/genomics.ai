import { Box } from "@mui/material";
import { colors } from "shared/theme/colors";

export default function Footer() {
    return (
        <Box sx={{bgcolor: colors.neutral[200], width: "100vw", height: "30vh"}}>
          
        </Box>
    )
}
